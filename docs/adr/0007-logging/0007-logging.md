# ロギング

## 変換画面

変換画面の `実行ボタン` をクリックすると、 `~/Library/Application Support/tree-image-optimizer/logs/app.${yyyyMMdd}.log` にログ出力してください。

- どんなコマンドを実行したのかが分かるようにする。
- コマンド実行に失敗した理由を解析できる形にログ出力する。
- 無限にログを残したくないので、最大30日文のファイルでローテートする。
